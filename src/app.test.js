const sum = require('./app');

// test('null',()=>{
//     let aNumber = 10
//     expect(aNumber).not.toBeNull()
//     expect(aNumber).toBe(110)
// })

describe("math tests", () => {

//     // 1 button -> reusable component 
//     // we can use this button to do several things like 
//     // Edit, delete, go to another page etc.
//     // We have 3 functions, edit, delete and update
//     // We will write 1 suite which will have 3 tests

    it("1 + 1 = 2", () => {
      expect(1 + 1).toBe(2);
    });
  
//     it("-1 * -1 !== -1", () => {
//       expect(-1 * -1).not.toBe(-1);
//     });
  
  });